# Hint 2

The file you're looking for is called `robots.txt`. It lives in the same folder as `index.html`.

Open it — either by navigating to `robots.txt` in your browser (if you're running a server), or by opening the file directly in a text editor or your browser's address bar.

Read through every `Disallow:` entry. These are paths on the site that the owner asked search engines to ignore. One of them is a path to an HTML file that's still live.

Navigate to that path directly — open it in your browser. Something is waiting there.

---

*[← Hint 1](hint-1.md) | [Back to challenge](index.html) | [Solution →](solution.md)*
