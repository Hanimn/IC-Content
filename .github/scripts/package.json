{
  "name": "ic-content-validators",
  "private": true,
  "description": "CI-only dependencies for .github/scripts/validate.mjs. Not user-facing — content authors never need to npm install. Versions are pinned to match IC-Page/agent-x's runtime so the CI check stays in lockstep with what Vercel actually runs.",
  "type": "module",
  "scripts": {
    "validate": "node validate.mjs"
  },
  "dependencies": {
    "@mdx-js/mdx": "^3.1.1",
    "remark-gfm": "^4.0.1",
    "gray-matter": "^4.0.3"
  }
}
